# Local development configuration

# Mongo (local Docker container)
MONGO_URI=mongodb://127.0.0.1:27017
MONGO_DB=quantraider

# JWT (local only; change for deployments)
JWT_SECRET=dev-secret-change-me
JWT_ISSUER=quantraider
JWT_AUDIENCE=quantraider
JWT_ACCESS_TTL_MINUTES=10080

# CORS (local frontend dev)
CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://api.samueltim.com,https://samueltim.com
